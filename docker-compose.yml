services:
  dittogen:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "5173:5173"
    command: sh -c "corepack enable && corepack prepare pnpm@9 --activate && pnpm install && pnpm --filter playground dev --host"
